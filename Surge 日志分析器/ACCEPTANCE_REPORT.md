# Surge 日志分析器当前验收报告

验收日期：2026-07-18

## 结论

- 当前功能范围：在 Scripting App 内选择日志文件、读取内容并完成解析与浏览。
- 本地自动化与源码验收：通过，14 项测试全部成功。
- Scripting App / 真实 iOS 验收：待按 `MANUAL_TEST_CHECKLIST.md` 执行。

## 当前项目结构

- `index.tsx`：唯一运行入口，呈现主界面并通过 `DocumentPicker.pickFiles()` 选择日志。
- `log-files.ts`：异步文本读取、iCloud 下载准备和内部诊断。
- `parser.ts`：日志解析、等级统计、搜索和时间窗选择。
- `types.ts`：日志、内存数据与 UI 状态类型。
- `script.json`：主入口与文件系统权限配置。

## 本地验收结果

### 自动化回归

- 命令：`npm test`
- 结果：14 项通过，0 项失败。
- 覆盖：App 生命周期、文件选择器资源释放、异步文本读取、iCloud 文件准备、解析基线、等级过滤、DEBUG、搜索、时间窗组合和 UI 性能结构。

当前 `_cache_log.txt` 大样本基线保持不变：

- 总行数：1987。
- 成功解析：1986 条。
- NOTICE：491 条。
- WARN：687 条。
- ERROR：808 条。
- DEBUG：0 条。
- 内存数据点：8 个。
- 内存统计：最小 3.27 MB，最大 23.64 MB，平均 17.02 MB，最新 23.64 MB。

### 生命周期与资源

- App 启动时不自动读取外部输入或历史缓存。
- 用户选择文件后，按需准备 iCloud 文件并异步读取文本内容。
- 文件选择器安全作用域在 `finally` 中释放。
- 页面关闭或异常时，外层 `finally` 始终调用 `Script.exit()`。
- 运行入口不使用同步文件 API。

### 配置检查

- `script.json` 入口为 `index.tsx`，`runInApp` 为 `true`。
- 当前项目不注册分享面板或快捷指令输入类型。
- 权限保留 `fileSystem` 与 `cloudSharedData`，用于读取本地和云端日志文件。

## 真机待验收

- 从 Scripting App 启动与关闭。
- 选择本地日志、iCloud 日志、空文件和不可读文件。
- 取消文件选择后保持现有状态。
- DEBUG 筛选、全部等级统计、搜索、滑块与时间窗。
- 约 2000 条和更大日志的打开速度、滚动和内存占用。
- 小屏、深色模式和动态字体。

完成真机清单前，不声明原生文件选择、权限或 UI 已通过设备验收。
